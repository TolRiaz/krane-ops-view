apiVersion: v1
kind: Service
metadata:
  labels:
    application: thkim-ops-view
    component: redis
  name: thkim-ops-view-redis
spec:
  selector:
    application: thkim-ops-view
    component: redis
  type: ClusterIP
  ports:
  - port: 6379
    protocol: TCP
    targetPort: 6379
